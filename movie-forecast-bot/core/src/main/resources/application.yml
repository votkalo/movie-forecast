server:
  port: 8081

bot:
  username: ${BOT_USERNAME}
  token: ${BOT_TOKEN}

geocoding:
  api-key: ${GEOCODING_API_KEY}

caffeine:
  movie:
    days: 1
    max-size: 1000

feign:
  movie-forecast-parser:
    url: http://localhost:8000
  movie-forecast-backend:
    url: http://localhost:8080